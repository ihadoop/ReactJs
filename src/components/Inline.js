import React from 'react';

const Inline = () => {
    const className={
        fontSize:'72px',
        color:'orange'
    }

    return (
        <div>
        <h1 style={className}>Inline</h1>
        </div>
    );
};

export default Inline;